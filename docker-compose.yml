services:
  go-backend:
    build:
      context: ./server
      dockerfile: Dockerfile

    ports:
      - 8080:8080

    environment:
      - ENV_A=1
      - ENV_B=2
      - ENV_C=3

    volumes:
      - ./server/:/server/
